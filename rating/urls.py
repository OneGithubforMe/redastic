from django.contrib import admin
from django.urls import path, include
from . import views

urlpatterns = [
    #path('rating',views.give_rating),
    #path('rating/edit/<int:rating_id>',views.edit_rating),
    #path('rating/delete/<int:rating_id>', views.delete_rating),
]


    